version: "3.8"

services:
  isomer:
    build: .
    container_name: isomer
    ports:
      - "27001:27001"
    volumes:
      - isomer_data:/data
    env_file:
      - .env
    environment:
      - ISOMER_DATA=/data
    restart: unless-stopped

volumes:
  isomer_data:
    driver: local
